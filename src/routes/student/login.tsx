import { createFileRoute } from "@tanstack/react-router";
import { LabForgeApp } from "@/lib/labforge";
export const Route = createFileRoute("/student/login")({ head: () => ({ meta: [{ title: "Student sign in — LabForge" }, { name: "description", content: "Sign in to the LabForge student workspace." }, { property: "og:title", content: "Student sign in — LabForge" }, { property: "og:description", content: "Sign in to the LabForge student workspace." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary" }] }), component: () => <LabForgeApp screen="studentLogin" /> });